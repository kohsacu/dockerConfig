version: "3.7"

services:
    http-proxy:
        env_file:
            - ./.env
        build:
            context: .
            dockerfile: Dockerfile
            args:
                - SQUID_VERSION=${SQUID_VERSION}
        container_name: ${CONTAINER}
        hostname: ${CONTAINER}
        image: ${REPOSITORY}:${TAG}
        restart: always
        tty: true
        volumes:
            - /etc/localtime:/etc/localtime:ro
            - /opt/docker.volume/${CONTAINER}/init.d:/opt/local/etc/init.d
            - /opt/docker.volume/${CONTAINER}/squid.etc:/opt/local/etc/squid
            - /opt/docker.volume/${CONTAINER}/squid.spool:/var/spool/squid
        logging:
            driver: "syslog"
            options:
                #syslog-address: udp://172.17.0.1:514
                syslog-facility: "daemon"
                tag: "{{.DaemonName}}/{{.ImageName}}/{{.Name}}"
        ports:
            - "${PUBLIC_SQUID}:3128"
        network_mode: "bridge"
